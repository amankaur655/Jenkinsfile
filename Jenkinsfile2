pipeline {
    agent any
    tools{
        maven 'M2_HOME'
    }
    stages{
        stage ('sonarqube scan'){
            steps {
                sh '.github/workflows/build.yml'
            }
        }
        stage ('maven commands'){
            steps{
                sh 'mvn clean compile install package'
            }
        }
    }
}